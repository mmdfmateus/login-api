const MissingParameterError = require('./missing-param-error');
const InvalidParameterError = require('../../utils/errors/invalid-param-error');

module.exports = { 
    MissingParameterError,
    InvalidParameterError
}